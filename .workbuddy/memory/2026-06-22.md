# 2026-06-22 工作日志

## AI 产品比赛项目 - 时光回声(TimeEcho)

- 用户参加 AI 产品比赛，比赛主题为"From Me to We"
- 老师示范：用户提问 → "十年前的自己"和"十年后的自己"分别回答
- 用户是艺术设计类学生，非技术专业
- 基于主题和示范推断方向：写给未来自己的信，AI 生成回信 + 回声匹配（发现他人也在问同样的问题）
- 用户确认方向正确 ✅
- 产出项目说明文档 `项目说明_TimeEcho.md`
- 创建 Next.js 项目（TypeScript + Tailwind CSS v4），手动搭建因 create-next-app 权限限制
- 完成三步核心流程：输入 → AI 回信(打字机效果) → 回声匹配
- 预置 12 条回声语料，关键词匹配算法
- 深色星空视觉氛围，打字机光标动画，卡片渐入效果
- 先用本地模板回复（14种情感维度），后改为接入真实 AI API
- DeepSeek API 余额不足(402)，切换为硅基流动(SiliconFlow)免费模型 Qwen2.5-72B
- 为部署到云端分享给朋友，将 API 调用改为客户端直连，构建为静态导出
- 部署到 CloudStudio（静态导出，但 API 路由不可用，链接失效）
- 改用 Cloudflare Tunnel 内网穿透：本地 dev server + cloudflared 暴露公网
- 公网地址：https://favors-planet-tin-rural.trycloudflare.com（cloudflared 进程存活期间有效，重启会变地址）
- 修复：恢复 /api/reply 服务端路由（AI 调用放服务端，API Key 不暴露），page.tsx 改为调用 /api/reply
- API Key 配置在 .env.local：SILICONFLOW_API_KEY=sk-bdg...（硅基流动免费额度）
- cloudflared 二进制在 /tmp/cloudflared（macOS ARM）
- 项目本地路径：/Users/zhangtianran/WorkBuddy/2026-06-22-15-46-25/time-echo/
- GitHub 仓库：zhangfugui0117-stack/nextjs-boilerplate

## 6/23 补充

- 次日服务停了需重启：先 `npm run dev`，再 `/tmp/cloudflared tunnel --url http://localhost:PORT`
- 新公网地址（6/23）：https://ftp-professor-would-tribe.trycloudflare.com（dev server 在 3001 端口）
- 修复：next.config.ts 去掉 output:export（会导致 API 路由失效），加 allowedDevOrigins 允许 trycloudflare 域名
- 修复：按钮 text-night 不是合法 Tailwind 类 → 改为 text-slate-900
- 硅基流动 API 也余额不足(403)，但代码有 fallback 到本地模板，不报错
- trycloudflare.com 在国内不需要梯子，Cloudflare 有国内节点
